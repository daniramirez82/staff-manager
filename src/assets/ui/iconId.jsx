
const IconId = ({ children }) => {
  return (
      <div className="absolute -top-6 w-16 h-16 flex justify-center items-center rounded bg-blue-500">
        {children}
      </div>
  );
};

export default IconId;
